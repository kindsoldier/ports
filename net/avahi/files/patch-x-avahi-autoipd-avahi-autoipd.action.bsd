--- ./avahi-autoipd/avahi-autoipd.action.bsd.orig	2010-08-26 03:51:38.960153000 +0300
+++ ./avahi-autoipd/avahi-autoipd.action.bsd	2015-01-02 15:33:56.000000000 +0200
@@ -32,11 +32,11 @@
 
 case "$1" in
 BIND)
-    ifconfig "$2" "$3"/16
+    ifconfig "$2" alias "$3"/16
     ;;
 
 CONFLICT|STOP|UNBIND)
-    ifconfig "$2" "$3"/16 delete
+    ifconfig "$2" -alias "$3"/16
     ;;
 
 *)
