#!/bin/sh -x

LDAP_DB_DIR="%%LDAP_DB_DIR%%"
#SLURP_DIR="%%SLURP_DIR%%"
LDAP_RUN_DIR="%%LDAP_RUN_DIR%%"

case $2 in 
    DEINSTALL)
	;;
    POST-DEINSTALL)
	for dir in ${LDAP_DB_DIR} ${LDAP_RUN_DIR}; do
	    rmdir ${dir} || ls -l ${dir}
	done

	;;
esac
#EOF
