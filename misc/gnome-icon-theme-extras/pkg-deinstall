#!/bin/sh

set -x

PREFIX=${PKG_PREFIX}
LOCALBASE=${PKG_PREFIX}

case $2 in 
    DEINSTALL)
	;;
    POST-DEINSTALL)
	${LOCALBASE}/bin/gtk-update-icon-cache -q ${PREFIX}/share/icons/gnome
	;;
esac
#EOF
