#!/bin/sh

case $2 in
    PRE-INSTALL)
	;;
    POST-INSTALL)
	${PKG_PREFIX}/bin/gtk-update-icon-cache -q -f ${PKG_PREFIX}/share/icons/matefaenzadark
	${PKG_PREFIX}/bin/gtk-update-icon-cache -q -f ${PKG_PREFIX}/share/icons/matefaenza
	;;
esac
#EOF
