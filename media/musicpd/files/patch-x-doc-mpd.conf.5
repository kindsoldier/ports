--- ./doc/mpd.conf.5.orig	2011-10-09 14:43:52.000000000 +0300
+++ ./doc/mpd.conf.5	2011-11-09 14:55:20.000000000 +0300
@@ -2,9 +2,9 @@
 .SH NAME
 mpd.conf \- Music Player Daemon configuration file
 .SH DESCRIPTION
-\fBmpd.conf\fP is the configuration file for mpd(1).  If not specified on the
-command line, MPD first searches for it at \fB~/.mpdconf\fP then at
-\fB~/.mpd/mpd.conf\fP and then in \fB/etc/mpd.conf\fP.
+\fBmusicpd.conf\fP is the configuration file for mpd(1).  If not specified on the
+command line, MPD first searches for it at \fB~/.musicpd.conf\fP then at
+\fB~/.musicpd/musicpd.conf\fP and then in \fB/usr/local/etc/musicpd.conf\fP.
 
 Lines beginning with a "#" character are comments.  All other non-empty lines
 specify parameters and their values.  These lines contain the parameter name
@@ -25,7 +25,7 @@
 Parameters that take a file or directory as an argument should use absolute
 paths.
 
-See \fBdocs/mpdconf.example\fP in the source tarball for an example
+See \fBdocs/mpusicd.conf.example\fP in the source tarball for an example
 configuration file.
 .SH REQUIRED PARAMETERS
 .TP
@@ -468,10 +468,10 @@
 This specifies the genre(s) of the stream.
 .SH FILES
 .TP
-.BI ~/.mpdconf
+.BI ~/.musicpd.conf
 User configuration file.
 .TP
-.BI /etc/mpd.conf
+.BI /usr/local/etc/musicpd.conf
 Global configuration file.
 .SH SEE ALSO
 mpd(1), mpc(1)
